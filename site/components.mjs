import {uiIcon} from '../assets/ui-icons.mjs';
export const glyph=uiIcon;
export const wordmark='<span>CleanNest</span>';
const links=[['services.html','Services','services','A clean for every corner'],['about.html','About us','people','Meet CleanNest'],['pricing.html','Pricing','price','Clear, upfront ranges'],['results.html','Our work & reviews','star','Results and customer experiences'],['areas-we-serve.html','Service areas','pin','Jalandhar & nearby cities'],['contact.html','Contact us','quote','Let’s talk'],['faqs.html','FAQs & journal','help','Answers and home care guides']];
export const navigation=active=>`<a class="skip-link" href="#main">Skip to content</a><header class="header"><div class="container header-inner"><nav class="desktop-nav" aria-label="Main navigation">${[['services.html','Services'],['pricing.html','Pricing']].map(([url,label])=>`<a href="${url}" ${active===url?'aria-current="page"':''}>${label}</a>`).join('')}</nav><a class="brand" href="/" aria-label="CleanNest home">${wordmark}</a><div class="header-actions"><a class="button primary header-quote" href="quote.html">Get a free quote</a><button class="menu-toggle" aria-label="Open navigation" aria-expanded="false" aria-controls="mobile-nav">${glyph('menu')}${glyph('close').replace('class="icon"','class="icon menu-close-icon"')}</button></div></div><nav id="mobile-nav" class="mobile-nav" aria-label="All pages" hidden inert><div class="menu-heading">A little help finding your way.</div><div class="menu-grid">${links.map(([url,label,icon,detail])=>`<a href="${url}"><span class="menu-icon">${glyph(icon)}</span><span><strong>${label}</strong><small>${detail}</small></span>${glyph()}</a>`).join('')}</div></nav></header>`;

export const simpleProcess=()=>`<section class="process-wrap"><div class="section container"><span class="eyebrow">THREE STEPS. NOTHING COMPLICATED.</span><h2>A clean home, made simple.</h2><ol class="process-journey" aria-label="How to book your clean">${[['quote','Get a quote','Tap what you need.'],['calendar','Book a date','Pick a day with us.'],['home','Enjoy your clean','We’ll handle the rest.']].map(([icon,title,caption],i)=>`<li class="journey-step"><div class="journey-icon">${glyph(icon)}<span class="journey-number" aria-hidden="true">${i+1}</span></div><h3>${title}</h3><p>${caption}</p>${i<2?`<span class="journey-arrow" aria-hidden="true">${glyph()}</span>`:''}</li>`).join('')}</ol></div></section>`;
export const simpleCare=()=>`<section class="care-section container"><div class="care-image"><img src="assets/img/editorial/sofa.webp" alt="Upholstery cleaning equipment on an ivory sofa" width="880" height="660" loading="lazy"></div><div class="care-copy"><span class="eyebrow">YOUR HOME. IN GOOD HANDS.</span><h2>Good people.<br>Thoughtful care.</h2><div class="care-benefits">${[['people','Trained team'],['shield','Background checked'],['leaf','Family-safe products'],['redo','Free re-clean promise']].map(([icon,text])=>`<div class="care-benefit"><span>${glyph(icon)}</span><h3>${text}</h3></div>`).join('')}</div><a class="text-link" href="about.html">Get to know CleanNest ${glyph()}</a></div></section>`;
