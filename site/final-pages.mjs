import {services,extras,groups,homes,money,chimneyPrice,supplements,faqs,faqSections,cities,slug} from './catalog.mjs';
import {businessInfo} from './business.mjs';
import {glyph} from './components.mjs';
import {zoomMap} from './zoom-map.mjs';
import {compactServiceCard,fromPrice,closingFinal,ratingPills,reviewData,googleProfile,justdialProfile} from './final-home.mjs';
const secondary=['mattress-steam-cleaning','curtain-cleaning','window-blinds-cleaning','carpet-steam-cleaning','refrigerator-cleaning','floor-renewal'];
export const pageOpening=(tag,title,text)=>`<section class="page-intro container"><span class="eyebrow">${tag}</span><h1>${title}</h1><p>${text}</p></section>`;
export function faqsFinal(){const sections=faqSections;const esc=s=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');return pageOpening('GOOD QUESTIONS. CLEAR ANSWERS.','A little clarity.<br>A lot of peace of mind.','Quick answers about the clean, the quote and your booking.')+`<section class="container faq-directory"><nav class="rate-nav" aria-label="Question topics">${sections.map(([name],i)=>`<a href="#faq-topic-${i}">${name}</a>`).join('')}</nav>${sections.map(([name,items],i)=>`<section class="faq-topic" id="faq-topic-${i}"><h2>${name}</h2><div class="faq-list">${items.map(([q,a])=>`<details><summary>${esc(q)}${glyph('chevron')}</summary><p>${esc(a)}</p></details>`).join('')}</div></section>`).join('')}<aside class="faq-help"><div><h2>Something else on your mind?</h2><p>Ask our local team. We’re available ${businessInfo.hours}.</p></div><a class="button primary" href="${businessInfo.whatsapp}" target="_blank" rel="noopener">${glyph('whatsapp')}Ask on WhatsApp</a></aside></section>${closingFinal()}`;}
export function areasFinal(){return pageOpening('JALANDHAR & NEARBY CITIES','Care, close to home.','Our Jalandhar team brings professional cleaning to homes and businesses across these thirteen cities.')+zoomMap()+`<section class="section container areas-page"><div class="section-heading"><div><span class="eyebrow">FIND YOUR NEIGHBOURHOOD</span><h2>Where we can help.</h2></div></div><nav class="city-page-links" aria-label="City cleaning service pages">${cities.map((c,i)=>`<a href="${slug(c)}.html">${glyph('pin')}<span>${c}${i===0?'<small>Our home city</small>':''}</span>${glyph()}</a>`).join('')}</nav><div class="area-enquiry"><div><h2>Just outside these areas?</h2><p>Send your locality and what needs cleaning. We’ll check availability and travel arrangements with you.</p></div><a class="button secondary" href="${businessInfo.whatsapp}" target="_blank" rel="noopener">${glyph('whatsapp')}Ask on WhatsApp</a></div></section>${closingFinal()}`;}
export function reviewsFinal(){const googleAction=googleProfile?`<a class="button secondary" href="${googleProfile}" target="_blank" rel="noopener"><span>Read Google reviews</span>${glyph('external')}</a>`:`<a class="button secondary" href="#google-review-excerpts"><span>Read Google review excerpts</span>${glyph()}</a>`;return pageOpening('WORDS FROM OUR CUSTOMERS','Good words.<br>Real experiences.','See what customers say about having CleanNest care for their spaces.')+`<section class="container reviews-page">${ratingPills()}<div class="review-platform-actions">${googleAction}<a class="button secondary" href="${justdialProfile}" target="_blank" rel="noopener"><span>Read Justdial reviews</span>${glyph('external')}</a></div><div class="section-heading" id="google-review-excerpts"><div><span class="eyebrow">FROM OUR GOOGLE REVIEWS</span><h2>Care that people remember.</h2></div></div><div class="dedicated-review-grid">${reviewData.map(([name,text])=>`<figure class="review"><span class="stars" role="img" aria-label="5 out of 5 stars">★★★★★</span><blockquote>“${text}”</blockquote><figcaption><span class="avatar">${name.split(' ').map(v=>v[0]).slice(0,2).join('').toUpperCase()}</span><span>${name}<small>Google review excerpt</small></span></figcaption></figure>`).join('')}</div><section class="platform-story"><div><span class="eyebrow">MORE LOCAL EXPERIENCES</span><h2>Find us on Justdial.</h2><p>Explore customer feedback on our Justdial profile, or share your own experience after a CleanNest visit.</p></div><a class="button primary" href="${justdialProfile}" target="_blank" rel="noopener"><span>Open Justdial</span>${glyph('external')}</a></section><p class="review-source-note">The Google excerpts shown here retain their source labels. A separate verified Google review-profile URL is pending owner confirmation; the Directions link continues to open the map.</p></section>${closingFinal()}`;}
const rateScope={bathroom:'Tiles, fittings, glass and descaling. Size and condition affect the final price.','kitchen-add':`Degreasing, surfaces and cabinets. Chimney is a separate ${chimneyPrice} extra.`,balcony:'Floors, railings and accessible surfaces.',chimney:'A separately selected cleaning service.','gas-stove':'Cooktop degreasing; included when booking kitchen cleaning.',ac:'Choose the cleaning treatment that fits your AC.',fridge:'Interior shelves, drawers and accessible surfaces.',cabinets:'Cabinet surfaces; size and condition affect the price.',fan:'Ceiling-fan dust and grime removal.',exhaust:'Grease and dust removal; included with kitchen cleaning.',sofa:'Price per seat. Chaise sections count as two seats.','dining-chairs':'Care for upholstered dining chairs.',cushions:'Per piece; included with sofa cleaning.',mattress:'Cleaning for a bare mattress.',carpet:'Measured carpet area.',curtains:'Per curtain panel; fabric and condition affect the price.',blinds:'Measured blind area.','commercial-floor':'Measured commercial floor area.','tile-floor':'Tile floor cleaning.',marble:'Marble floor care.',granite:'Granite floor care.','italian-marble':'Material and treatment assessed before booking.','post-construction':'Cleaning after construction.','jet-washing':'Outdoor pressure washing.'};
const rateCell=(low,unit='',high)=>`<strong>From ${money(low)}</strong>${unit?`<span>${unit}</span>`:''}${high?`<small>Up to ${money(high)}*</small>`:''}`;
const popularRate=id=>groups.flatMap(g=>g.items).find(r=>r.id===id);
const popularPrice=r=>r.high?`${money(r.base)}–${money(r.high)} ${r.unit}`:`From ${money(r.base)} ${r.unit}`;
export function pricingFinal(){const popular=[['1 BHK',homes['1'].label],['2 BHK',homes['2'].label],['Bathroom',popularPrice(popularRate('bathroom'))],['Kitchen',popularPrice(popularRate('kitchen-add'))],['Sofa',`From ${money(popularRate('sofa').base)} / seat`]];return pageOpening('DEEP CLEANING PRICES · JALANDHAR','Deep cleaning price list<br>in Jalandhar','Good care. Clear prices. Published starting prices for homes, kitchens, bathrooms, appliances and more.')+`<section class="container rate-page"><section class="popular-prices" aria-labelledby="popular-prices-title"><div><span class="eyebrow">START HERE</span><h2 id="popular-prices-title">Most popular prices</h2><p>A quick view of the services people ask about most. See the full rate card below for every option and unit.</p></div><div class="popular-price-grid">${popular.map(([label,price])=>`<a href="quote.html"><span>${label}</span><strong>${price}</strong>${glyph()}</a>`).join('')}</div></section><div class="rate-intro"><p>*Ranges reflect size, condition and scope. We agree the final price before booking.</p><a class="button primary" href="quote.html">Build My Estimate ${glyph()}</a></div><nav class="rate-nav" aria-label="Pricing categories"><a href="#home-prices">Full home</a>${groups.map((g,i)=>`<a href="#rate-group-${i}">${['Kitchen & bathroom','Appliances','Furnishings','Floors & specialist'][i]}</a>`).join('')}</nav><section class="rate-section" id="home-prices"><div class="rate-heading"><span class="rate-number">01</span><div><h2>Full-home deep cleaning</h2><p>Kitchen, bathrooms, floors and interior surfaces in the agreed scope.</p></div></div><table class="rate-table"><caption class="sr-only">Full-home deep cleaning prices</caption><thead><tr><th scope="col">Home size</th><th scope="col">Price</th></tr></thead><tbody>${Object.entries(homes).map(([key,r])=>`<tr><th scope="row"><a href="quote.html?service=full-house-cleaning&home=${key}">${key==='villa'?'5+ bedrooms / villa':key+' BHK'} ${glyph()}</a><small>${key==='villa'?'A quote matched to your property.':'Kitchen and bathroom cleaning included.'}</small></th><td>${key==='villa'?'<strong>Custom quote</strong>':rateCell(r.low,'/ home',r.high)}</td></tr>`).join('')}</tbody></table></section>${groups.map((g,i)=>`<section class="rate-section" id="rate-group-${i}"><div class="rate-heading"><span class="rate-number">0${i+2}</span><div><h2>${g.name}</h2></div></div><table class="rate-table"><caption class="sr-only">${g.name} pricing</caption><thead><tr><th scope="col">Service & care</th><th scope="col">Price</th></tr></thead><tbody>${g.items.flatMap(r=>(r.tiers||[{label:'',p:r.base}]).map(t=>`<tr><th scope="row">${r.label}${t.label?`<span class="rate-option">${t.label}</span>`:''}<small>${rateScope[r.id]||''}</small></th><td>${rateCell(t.p,r.unit,r.tiers?null:r.high)}</td></tr>`)).join('')}</tbody></table></section>`).join('')}<aside class="pricing-note"><h2>A few useful details.</h2><p>Recliner seats add ${money(supplements.recliner)} each. Chimney cleaning is optional at ${chimneyPrice}. Pool, chandelier, newborn preparation, sanitisation, regular and post-renovation cleaning receive a custom quote.</p><p>50% confirms your booking. The remaining 50% is payable after service.</p><a class="text-link" href="quote.html">Choose your services ${glyph()}</a></aside></section>${closingFinal()}`;}
export function servicesFinal(){const main=[...services.slice(0,4),services[5],services[4]],more=secondary.map(id=>services.find(s=>s.id===id)),other=services.filter(s=>!main.includes(s)&&!more.includes(s)&&!['gas-stove-cleaning','exhaust-fan-cleaning'].includes(s.id));return pageOpening('SERVICE DIRECTORY','Explore CleanNest<br>cleaning services','Compare service scopes and starting prices for homes, businesses, rooms, furnishings, appliances and specialist floor care. For local availability, see our Jalandhar service-area page.')+`<section class="container service-directory-final"><h2 class="sr-only">Popular cleaning services</h2><p class="directory-note"><a class="text-link" href="jalandhar.html">Cleaning availability and local contact information for Jalandhar ${glyph()}</a></p><div class="service-grid">${main.map(s=>compactServiceCard(s,s.id==='full-house-cleaning')).join('')}</div><section class="secondary-services"><div class="section-heading"><div><span class="eyebrow">A LITTLE EXTRA CARE</span><h2>More ways to refresh.</h2></div></div><div class="secondary-grid">${more.map(s=>`<article class="secondary-service"><a class="secondary-photo" href="${s.id}.html"><img src="${s.image}" alt="${s.name} service illustration" width="640" height="480" loading="lazy"></a><div><h3><a href="${s.id}.html">${s.name}</a></h3><p>${fromPrice(s)} <small>${groups.flatMap(g=>g.items).find(r=>r.id===s.rate)?.unit||''}</small></p><a class="text-link" href="quote.html?service=${s.id}" aria-label="Get a quote for ${s.name}">Get a quote ${glyph()}</a></div></article>`).join('')}</div></section><section class="other-services"><span class="eyebrow">FOR THE REST OF YOUR SPACE</span><h2>Other services.</h2><p>Specialist care, without the long checklist.</p><div class="other-service-list">${other.map(s=>`<a href="${s.id}.html"><span>${s.name}</span><strong>${fromPrice(s)}<small>${groups.flatMap(g=>g.items).find(r=>r.id===s.rate)?.unit||''}</small></strong>${glyph()}</a>`).join('')}</div><p class="directory-note">Small extras such as ceiling fans, storage cabinets and dining chairs are available in your quote. Kitchen chimney cleaning is optional at ${chimneyPrice}.</p></section></section>${closingFinal()}`;}
